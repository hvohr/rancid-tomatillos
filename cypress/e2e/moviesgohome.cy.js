describe('template spec', () => {
  it('passes', () => {
    cy.visit('https://example.cypress.io')
  })
})
describe('Movies go home should test', () => {
  it('Movies go home tests', () => {
    expect(true).to.equal(true)
  })
})

